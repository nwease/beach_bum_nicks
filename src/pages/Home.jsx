import React from 'react';
import Header from '../components/home/Header';

const Home = () => {
    return (
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};

export default Home;
